{
  "name": "app",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm run start && gulp",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "express": "^4.16.2",
    "mongoose": "^5.0.9",
    "multer": "^1.3.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
